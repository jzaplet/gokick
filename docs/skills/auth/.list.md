---
layout: 'list'
uri: '/skills/auth'
position: 60
slug: 'skills-auth'
parent: 'skills'
navTitle: 'Auth & bezpečnost'
title: 'Auth & bezpečnost'
description: 'Autentizace a zabezpečení — JWT + refresh, permissions, rate limiting, CSRF a hardening.'
---

# Auth & bezpečnost

Kdo se přihlásí a co smí: JWT access + refresh s rotací (`/gk-auth`), permissions
(`/gk-permissions`), rate limiting a brute-force lock (`/gk-rate-limiting`) a hardening
hrany serveru (`/gk-hardening`).
