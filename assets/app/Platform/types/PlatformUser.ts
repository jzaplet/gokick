// Code generated by tsgen; DO NOT EDIT.

import { isBoolean, isRecord, isString, nullable } from '@/app-ui/Fetch/guards';

export type PlatformUser = {
    id: string;
    nickname: string;
    email: string;
    role: string;
    active: boolean;
    tenant_id: string;
    tenant_name: string;
    last_login_at: string | null;
};

export const isPlatformUser = (v: unknown): v is PlatformUser =>
    isRecord(v)
    && isString(v['id'])
    && isString(v['nickname'])
    && isString(v['email'])
    && isString(v['role'])
    && isBoolean(v['active'])
    && isString(v['tenant_id'])
    && isString(v['tenant_name'])
    && nullable(isString)(v['last_login_at']);
